{
  "name": "intellipath",
  "version": "1.0.0",
  "private": true,
  "description": "Intellipath - Intelligent career path recommendation system",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix mobile install && python -m pip install -r ml-service/requirements.txt",
    "backend": "npm --prefix backend run dev",
    "mobile": "npm --prefix mobile start",
    "ml": "python ml-service/app.py"
  }
}
